<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysRoleMenuMapper">

	<resultMap type="SysRoleMenu" id="SysRoleMenuResult">
		<result property="roleId"     column="role_id"      />
		<result property="menuId"     column="menu_id"      />
	</resultMap>
	
	<select id="checkMenuExistRole" resultType="Integer">
	    select count(1) from sys_role_menu where menu_id = #{menuId}
	</select>

	<delete id="deleteRoleMenuByRoleId" parameterType="Long">
		delete from sys_role_menu where role_id=#{roleId}
	</delete>

	<delete id="deleteRoleMenuByMenuId" parameterType="Long">
		delete from sys_role_menu where menu_id=#{menuId}
	</delete>
	
	<delete id="deleteRoleMenu" parameterType="Long">
 		delete from sys_role_menu where role_id in
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId}
        </foreach> 
 	</delete>
	
	<insert id="batchRoleMenu">
		insert into sys_role_menu(role_id, menu_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.roleId},#{item.menuId})
		</foreach>
	</insert>

	<insert id="copyRoleMenuBySourceMenu">
		insert ignore into sys_role_menu(role_id, menu_id)
		select role_id, #{targetMenuId}
		from sys_role_menu
		where menu_id = #{sourceMenuId}
	</insert>

	<delete id="deleteRoleMenuByAllAutoTagMenus">
		delete from sys_role_menu
		where menu_id in (
			select menu_id from sys_menu where remark = 'AUTO_TAG_MENU'
		)
	</delete>

	<delete id="deleteRoleMenuByAutoTagMenusNotInPaths">
		delete from sys_role_menu
		where menu_id in (
			select menu_id
			from sys_menu
			where remark = 'AUTO_TAG_MENU'
			  and path not in
			<foreach item="path" collection="list" open="(" separator="," close=")">
				#{path}
			</foreach>
		)
	</delete>
	
</mapper>
